{
  "mcpServers": {
    "chaingpt": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/launch.mjs"],
      "env": {
        "CHAINGPT_API_KEY": "${CHAINGPT_API_KEY}"
      }
    }
  }
}
