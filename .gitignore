# Dependencies
node_modules/

# Build output
dist/
# …except the MCP server's dist, shipped prebuilt because Claude Code runs no
# build step on plugin install. Kept in sync with src by the build-check CI.
!mcp-server/dist/
!mcp-server/dist/**

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Lock files (keep package.json, let users generate their own)
yarn.lock
pnpm-lock.yaml
